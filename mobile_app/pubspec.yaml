name: mobile_app
description: CCTV Security Monitor Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI Components
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  
  # HTTP & API
  http: ^1.1.0
  dio: ^5.4.0
  
  # State Management
  provider: ^6.1.1
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # Image Handling
  cached_network_image: ^3.3.0
  
  # Date & Time
  intl: ^0.18.1
  
  # Icons
  flutter_svg: ^2.0.9
  
  # Loading Indicators
  flutter_spinkit: ^5.2.0
  
  # Pull to Refresh
  pull_to_refresh: ^2.0.0
  
  # Push Notifications (Optional)
  # firebase_messaging: ^14.7.6
  
  # Local Notifications
  # flutter_local_notifications: ^16.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Add assets (images, icons, etc.)
  # assets:
  #   - assets/images/
  #   - assets/icons/